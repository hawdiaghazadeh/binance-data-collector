httpx>=0.27.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
pyyaml>=6.0.1
clickhouse-connect>=0.7.0
tqdm>=4.66.0
structlog>=24.1.0
packaging>=24.0

# Docker images (downloader/importer) install from this file only.
# Platform + RL: pip install -e ".[dev]"  (adds torch, gymnasium, pytest, …)
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
respx>=0.21.0
